# 🔧 Исправление divergent branches

## ❌ Проблема

При попытке `git pull` появляется ошибка:

```
hint: You have divergent branches and need to specify how to reconcile them.
fatal: Need to specify how to reconcile divergent branches.
```

Это означает, что локальная ветка и удаленная ветка разошлись (divergent branches).

---

## ✅ Решение

### Способ 1: Принудительное обновление (РЕКОМЕНДУЕТСЯ для сервера)

```bash
cd ~/novolunie
git fetch origin main
chmod +x force-pull.sh
./force-pull.sh
```

Или вручную:
```bash
cd ~/novolunie
git fetch origin main
git reset --hard origin/main
```

Это **полностью перезапишет** локальную ветку удаленной.

---

### Способ 2: Автоматический скрипт (с merge/rebase)

```bash
cd ~/novolunie
git pull origin main
chmod +x fix-divergent-branches.sh
./fix-divergent-branches.sh
```

Скрипт:
1. Создаст резервную копию
2. Отменит локальные изменения
3. Подтянет из Git
4. Проверит результат

---

### Способ 3: Вручную (быстро)

**Если на сервере НЕ нужно сохранять локальные изменения:**

```bash
cd ~/novolunie

# Отменить локальные изменения и подтянуть из Git
git reset --hard HEAD
git pull origin main
```

**Или принудительно перезаписать локальную ветку:**

```bash
cd ~/novolunie

# Получить изменения
git fetch origin main

# Перезаписать локальную ветку
git reset --hard origin/main
```

---

### Способ 3: Через merge (если нужно сохранить локальные изменения)

```bash
cd ~/novolunie

# Настроить merge как стратегию по умолчанию
git config pull.rebase false

# Подтянуть изменения
git pull origin main
```

---

### Способ 4: Через rebase (если нужно сохранить локальные изменения)

```bash
cd ~/novolunie

# Настроить rebase как стратегию по умолчанию
git config pull.rebase true

# Подтянуть изменения
git pull origin main
```

---

## 🎯 Рекомендация для сервера

**На сервере обычно НЕ нужно сохранять локальные изменения**, поэтому используйте:

```bash
cd ~/novolunie
git reset --hard HEAD
git pull origin main
```

Или скрипт:
```bash
./fix-divergent-branches.sh
```

---

## ⚠️ Важно

### Если на сервере есть важные локальные изменения:

1. **Сохраните их перед reset:**
   ```bash
   # Создать резервную копию
   cp -r ~/novolunie ~/novolunie-backup
   
   # Или закоммитить локально
   git add .
   git commit -m "Local changes backup"
   ```

2. **Затем подтяните из Git:**
   ```bash
   git pull origin main
   ```

3. **Если есть конфликты, разрешите их вручную**

---

## 🔄 Быстрое исправление (одна команда)

```bash
cd ~/novolunie && git reset --hard HEAD && git pull origin main
```

---

## 📋 Проверка после исправления

```bash
# Проверка статуса
git status

# Проверка последних коммитов
git log --oneline -5

# Проверка файлов
ls -la assets/logo.png
```

---

## 🆘 Если ничего не помогает

### Полная переустановка из Git:

```bash
cd ~/novolunie

# Сохранить конфигурацию (если есть)
cp nginx.conf /tmp/nginx.conf.backup 2>/dev/null || true

# Удалить всё и клонировать заново
cd ~
rm -rf novolunie
git clone https://github.com/som1one/Novolunie-delta.git novolunie
cd novolunie

# Восстановить конфигурацию (если нужно)
cp /tmp/nginx.conf.backup nginx.conf 2>/dev/null || true
```

---

## ✅ Рекомендация

**Для сервера используйте скрипт** - он безопаснее и создает резервную копию:

```bash
./fix-divergent-branches.sh
```
