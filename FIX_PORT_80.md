# 🔧 Исправление: Сервер исключен из балансировки (порт 80)

## Проблема
Балансировщик нагрузки не может достучаться до порта 80 на сервере.

## Быстрое решение

### Автоматический способ:

```bash
cd ~/novolunie
git pull origin main
chmod +x fix-port-80.sh
./fix-port-80.sh
```

### Ручной способ:

#### 1. Откройте порты в файрволе

```bash
# Проверьте статус UFW
sudo ufw status

# Если UFW активен, откройте порты
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Проверьте правила
sudo ufw status numbered
```

#### 2. Остановите системный Nginx (если запущен)

```bash
# Проверьте, запущен ли системный Nginx
sudo systemctl status nginx

# Если запущен, остановите его (он не нужен, если используется Docker)
sudo systemctl stop nginx
sudo systemctl disable nginx
```

#### 3. Проверьте, что контейнер запущен и слушает порты

```bash
cd ~/novolunie

# Проверьте статус
docker compose ps

# Если контейнер не запущен
docker compose up -d

# Проверьте проброшенные порты
docker port novolunie-web

# Должно быть:
# 80/tcp -> 0.0.0.0:80
# 443/tcp -> 0.0.0.0:443
```

#### 4. Проверьте, что порты слушаются на хосте

```bash
# Проверка порта 80
sudo netstat -tlnp | grep ":80 "

# Должно быть что-то вроде:
# tcp  0  0  0.0.0.0:80  0.0.0.0:*  LISTEN  12345/docker-proxy

# Проверка порта 443
sudo netstat -tlnp | grep ":443 "
```

#### 5. Проверьте доступность локально

```bash
# HTTP должен возвращать 301 (редирект на HTTPS)
curl -I http://localhost

# HTTPS должен возвращать 200
curl -I https://localhost
```

#### 6. Перезапустите контейнер

```bash
cd ~/novolunie
docker compose restart

# Подождите 5 секунд
sleep 5

# Проверьте статус
docker compose ps
docker compose logs --tail=20 web
```

## Проверка после исправления

### 1. С сервера

```bash
# HTTP редирект
curl -I http://localhost
# Должен вернуть: HTTP/1.1 301 Moved Permanently

# HTTPS
curl -I https://localhost
# Должен вернуть: HTTP/2 200
```

### 2. Снаружи (с вашего компьютера)

```bash
# Замените IP на IP вашего сервера
curl -I http://85.239.44.197
# Должен вернуть: HTTP/1.1 301 Moved Permanently

curl -I https://e-novolunie.ru
# Должен вернуть: HTTP/2 200
```

### 3. Проверка портов

```bash
# Проверка порта 80
telnet 85.239.44.197 80
# Или
nc -zv 85.239.44.197 80

# Проверка порта 443
telnet 85.239.44.197 443
# Или
nc -zv 85.239.44.197 443
```

## Частые проблемы

### Проблема 1: Файрвол провайдера блокирует порты

**Решение:** Откройте порты 80 и 443 в панели управления вашего хостинг-провайдера.

### Проблема 2: Системный Nginx занимает порт 80

**Решение:**
```bash
sudo systemctl stop nginx
sudo systemctl disable nginx
docker compose restart
```

### Проблема 3: Контейнер не запущен

**Решение:**
```bash
cd ~/novolunie
docker compose up -d
docker compose ps
docker compose logs web
```

### Проблема 4: Ошибка в конфигурации Nginx

**Решение:**
```bash
docker compose exec web nginx -t
# Если есть ошибки, проверьте nginx.conf
```

### Проблема 5: Порт занят другим процессом

**Решение:**
```bash
# Найдите процесс на порту 80
sudo lsof -i :80

# Остановите процесс (замените PID)
sudo kill -9 <PID>

# Перезапустите контейнер
docker compose restart
```

## Диагностика

Если проблема не решена, выполните полную диагностику:

```bash
cd ~/novolunie
chmod +x fix-port-80.sh
./fix-port-80.sh
```

Скрипт проверит:
- ✅ Статус контейнера
- ✅ Проброшенные порты
- ✅ Файрвол (UFW)
- ✅ Системный Nginx
- ✅ Другие процессы на портах
- ✅ Доступность локально и снаружи
- ✅ Конфигурацию Nginx

## После исправления

Балансировщик нагрузки автоматически вернет сервер в балансировку после успешной проверки порта 80. Обычно это занимает 1-2 минуты.

Проверьте статус в панели управления балансировщиком нагрузки.
