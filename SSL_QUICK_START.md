# ⚡ Быстрая настройка бесплатного SSL

## 🎯 Что делать (5 шагов)

### 1. Подключитесь к серверу
```bash
ssh root@ваш_сервер
cd ~/novolunie
```

### 2. Обновите код и запустите скрипт
```bash
git pull origin main
chmod +x install-ssl-dns.sh
sudo ./install-ssl-dns.sh
```

### 3. Добавьте TXT записи в DNS

Certbot покажет что-то вроде:
```
Please deploy a DNS TXT record:
_acme-challenge.e-novolunie.ru
Значение: 00XvlVkxaIwO-aCcozQWZKvu54777yN1QGY9x7LZERg
```

**Что делать:**
1. Откройте https://timeweb.cloud
2. **Домены** → **e-novolunie.ru** → **DNS**
3. Добавьте TXT запись:
   - **Тип:** TXT
   - **Имя:** `_acme-challenge` (или `_acme-challenge.e-novolunie.ru`)
   - **Значение:** (скопируйте из certbot)
4. Сохраните
5. Подождите 2-5 минут
6. Вернитесь в терминал и нажмите **Enter**

### 4. Готово! ✅

Скрипт автоматически:
- Получит сертификат
- Настроит Nginx с HTTPS
- Скопирует файлы сайта

### 5. Проверьте
```bash
curl -I https://e-novolunie.ru
```

Должен вернуть `200 OK`.

---

## ❌ Если что-то пошло не так

### Certbot не может получить сертификат
- Проверьте, что TXT записи добавлены правильно
- Подождите дольше (до 10 минут)
- Проверьте через: `dig TXT _acme-challenge.e-novolunie.ru`

### Сайт не открывается по HTTPS
```bash
# Проверьте Nginx
sudo nginx -t
sudo systemctl status nginx

# Проверьте порты
sudo netstat -tlnp | grep 443
```

---

## 📝 Полная инструкция

См. `INSTALL_SSL_MANUAL.md` для детальной информации.
