# 🔐 Настройка аутентификации Git на сервере

## ❌ Проблема

GitHub больше **не поддерживает пароли** для Git операций. При попытке пушить появляется ошибка:

```
remote: Invalid username or token. Password authentication is not supported for Git operations.
```

---

## ✅ Решение: 2 способа

### Способ 1: Personal Access Token (проще)

#### Шаг 1: Создайте токен в GitHub

1. Откройте: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Настройки:
   - **Note:** `Novolunie Server`
   - **Expiration:** `90 days` (или `No expiration`)
   - **Scopes:** отметьте **`repo`** (все подразделы)
4. Нажмите **"Generate token"**
5. **СКОПИРУЙТЕ токен** (показывается только один раз!)

#### Шаг 2: Используйте токен на сервере

**Вариант А: Автоматический скрипт**
```bash
cd ~/novolunie
git pull origin main
chmod +x setup-git-auth.sh
./setup-git-auth.sh
```

**Вариант Б: Вручную**
```bash
cd ~/novolunie

# Замените YOUR_TOKEN на ваш токен
git remote set-url origin https://YOUR_TOKEN@github.com/som1one/Novolunie-delta.git

# Теперь можно пушить
git push origin main
```

**Вариант В: Через Git Credential Helper (рекомендуется)**
```bash
cd ~/novolunie

# Настройка credential helper
git config --global credential.helper store

# При первом пуше введите:
# Username: som1ones
# Password: ВАШ_ТОКЕН (не пароль!)

# Токен сохранится и больше не нужно будет вводить
```

---

### Способ 2: SSH ключ (безопаснее)

#### Шаг 1: Создайте SSH ключ на сервере

```bash
# Создание ключа
ssh-keygen -t ed25519 -C "your_email@example.com"

# Нажмите Enter для всех вопросов (или введите пароль)
```

#### Шаг 2: Скопируйте публичный ключ

```bash
cat ~/.ssh/id_ed25519.pub
```

Скопируйте весь вывод (начинается с `ssh-ed25519`).

#### Шаг 3: Добавьте ключ в GitHub

1. Откройте: https://github.com/settings/ssh/new
2. **Title:** `Novolunie Server`
3. **Key:** вставьте скопированный ключ
4. Нажмите **"Add SSH key"**

#### Шаг 4: Настройте Git для использования SSH

```bash
cd ~/novolunie
git remote set-url origin git@github.com:som1one/Novolunie-delta.git

# Проверка
ssh -T git@github.com
# Должно показать: "Hi som1one! You've successfully authenticated..."
```

#### Шаг 5: Теперь можно пушить

```bash
git push origin main
```

---

## 🚀 Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

```bash
cd ~/novolunie
git pull origin main
chmod +x setup-git-auth.sh
./setup-git-auth.sh
```

Скрипт спросит, какой способ использовать, и настроит всё автоматически.

---

## 📋 Быстрая настройка (если уже есть токен)

```bash
cd ~/novolunie

# Замените YOUR_TOKEN на ваш токен
git remote set-url origin https://YOUR_TOKEN@github.com/som1one/Novolunie-delta.git

# Проверка
git remote -v

# Теперь можно пушить
git push origin main
```

---

## ⚠️ Безопасность

### Personal Access Token:
- ✅ Проще настроить
- ⚠️ Токен виден в `git remote -v` (можно скрыть через credential helper)
- ⚠️ Нужно обновлять при истечении

### SSH ключ:
- ✅ Безопаснее (приватный ключ не передается)
- ✅ Не нужно вводить токен каждый раз
- ✅ Можно использовать один ключ для всех репозиториев

---

## 🔄 Если токен истек

### Проверка текущего remote:
```bash
git remote -v
```

### Обновление токена:
```bash
# Создайте новый токен в GitHub
# Затем обновите URL:
git remote set-url origin https://NEW_TOKEN@github.com/som1one/Novolunie-delta.git
```

---

## ✅ Рекомендация

**Для сервера лучше использовать SSH ключ** - он безопаснее и не требует обновления.

**Для быстрой настройки используйте Personal Access Token.**

---

## 🆘 Если что-то не работает

### Проверка текущей настройки:
```bash
git remote -v
```

### Проверка SSH подключения:
```bash
ssh -T git@github.com
```

### Проверка Git конфигурации:
```bash
git config --list | grep credential
```

---

## 📚 Полезные ссылки

- GitHub Personal Access Tokens: https://github.com/settings/tokens
- GitHub SSH Keys: https://github.com/settings/ssh
- Git Credential Helper: https://git-scm.com/docs/git-credential
