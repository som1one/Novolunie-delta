# 🔧 Исправление ошибки 503 для домена e-novolunie.ru

## Диагностика проблемы

### 1. Проверьте статус Docker контейнера

```bash
# На сервере
docker compose ps
docker compose logs web
```

### 2. Проверьте, что контейнер запущен

```bash
# Если контейнер не запущен, запустите его
cd ~/novolunie
docker compose up -d

# Проверьте статус
docker compose ps
```

### 3. Проверьте порты

```bash
# Проверьте, что порт 80 свободен
sudo netstat -tulpn | grep :80

# Или
sudo ss -tulpn | grep :80
```

### 4. Проверьте конфигурацию Nginx

```bash
# Проверьте синтаксис конфигурации внутри контейнера
docker compose exec web nginx -t
```

### 5. Проверьте DNS

```bash
# Проверьте, что домен указывает на правильный IP
nslookup e-novolunie.ru
dig e-novolunie.ru

# Должен вернуть: 85.239.44.197
```

## Решение проблемы

### Шаг 1: Обновите код из Git

```bash
cd ~/novolunie
git pull origin main
```

### Шаг 2: Пересоберите контейнер

```bash
docker compose down
docker compose up -d --build
```

### Шаг 3: Проверьте логи

```bash
# Логи Nginx
docker compose logs web

# Логи в реальном времени
docker compose logs -f web
```

### Шаг 4: Проверьте доступность

```bash
# С сервера
curl -I http://e-novolunie.ru
curl -I http://localhost

# Проверьте заголовки
curl -v http://e-novolunie.ru
```

## Если проблема сохраняется

### Вариант 1: Проверьте файрвол

```bash
# Проверьте правила UFW
sudo ufw status

# Если порт 80 закрыт, откройте его
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw reload
```

### Вариант 2: Проверьте, не запущен ли другой веб-сервер

```bash
# Проверьте, не запущен ли системный Nginx
sudo systemctl status nginx

# Если запущен, остановите его
sudo systemctl stop nginx
sudo systemctl disable nginx
```

### Вариант 3: Перезапустите Docker

```bash
# Перезапустите Docker daemon
sudo systemctl restart docker

# Перезапустите контейнеры
cd ~/novolunie
docker compose restart
```

### Вариант 4: Проверьте права доступа

```bash
# Проверьте права на файлы
ls -la ~/novolunie/

# Убедитесь, что файлы доступны
chmod -R 755 ~/novolunie/
```

## Настройка DNS (если еще не настроено)

Убедитесь, что в DNS записях домена указано:

```
A запись: e-novolunie.ru -> 85.239.44.197
A запись: www.e-novolunie.ru -> 85.239.44.197
```

## Проверка после исправления

```bash
# 1. Проверьте статус контейнера
docker compose ps

# 2. Проверьте логи
docker compose logs web | tail -20

# 3. Проверьте доступность
curl -I http://e-novolunie.ru

# Должен вернуть: HTTP/1.1 200 OK
```

## Быстрое исправление (одной командой)

```bash
cd ~/novolunie && \
git pull origin main && \
docker compose down && \
docker compose up -d --build && \
sleep 3 && \
docker compose ps && \
curl -I http://e-novolunie.ru
```
