#!/bin/bash

# Скрипт для коммита и пуша изменений в Git
# ⚠️ ВНИМАНИЕ: Обычно изменения делают локально, а на сервере только pull!

echo "📤 Коммит и пуш изменений в Git"
echo "================================"
echo ""

cd ~/novolunie || exit 1

echo "1️⃣ Проверка статуса Git..."
git status

echo ""
echo "2️⃣ Показываем изменения..."
git diff --stat

echo ""
read -p "Продолжить? (y/n): " -n 1 -r
echo ""
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ Отменено"
    exit 1
fi

echo ""
echo "3️⃣ Добавление всех изменений..."
git add .

echo ""
echo "4️⃣ Введите сообщение коммита:"
read -p "Commit message: " COMMIT_MSG

if [ -z "$COMMIT_MSG" ]; then
    COMMIT_MSG="Update from server $(date '+%Y-%m-%d %H:%M:%S')"
    echo "Используется сообщение по умолчанию: $COMMIT_MSG"
fi

echo ""
echo "5️⃣ Создание коммита..."
git commit -m "$COMMIT_MSG"

echo ""
echo "6️⃣ Отправка в Git..."
git push origin main

echo ""
echo "✅ Готово! Изменения отправлены в Git"
echo ""
echo "Теперь на локальной машине выполните:"
echo "  git pull origin main"
