services:
  web:
    build: .
    container_name: novolunie-web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
    networks:
      - novolunie-network

networks:
  novolunie-network:
    driver: bridge