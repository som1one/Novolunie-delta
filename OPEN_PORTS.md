# 🔓 Открытие портов 80 и 443

## Быстрое решение

### Автоматический способ:

```bash
cd ~/novolunie
git pull origin main
chmod +x open-ports.sh
./open-ports.sh
```

## Ручной способ

### 1. UFW (Ubuntu Firewall) - самый распространённый

```bash
# Проверьте статус
sudo ufw status

# Если UFW активен, откройте порты
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Проверьте правила
sudo ufw status numbered

# Если UFW не активен, но нужно его включить:
sudo ufw --force enable
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

### 2. iptables (если используется вместо UFW)

```bash
# Откройте порты
sudo iptables -I INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -I INPUT -p tcp --dport 443 -j ACCEPT

# Сохраните правила (чтобы не потерялись после перезагрузки)
sudo iptables-save | sudo tee /etc/iptables/rules.v4

# Или установите пакет для автоматического сохранения
sudo apt install iptables-persistent -y
sudo netfilter-persistent save
```

### 3. firewalld (CentOS/RHEL)

```bash
# Откройте порты
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload

# Проверьте
sudo firewall-cmd --list-ports
```

## Проверка после открытия портов

### 1. Проверка на сервере

```bash
# Проверьте, что порты слушаются
sudo netstat -tlnp | grep -E ":(80|443) "

# Должно быть что-то вроде:
# tcp  0  0  0.0.0.0:80  0.0.0.0:*  LISTEN  12345/docker-proxy
# tcp  0  0  0.0.0.0:443  0.0.0.0:*  LISTEN  12345/docker-proxy

# Проверьте доступность локально
curl -I http://localhost
# Должен вернуть: HTTP/1.1 301 Moved Permanently

curl -I https://localhost
# Должен вернуть: HTTP/2 200
```

### 2. Проверка снаружи

```bash
# Узнайте IP сервера
curl ifconfig.me

# Проверьте с вашего компьютера
curl -I http://85.239.44.197
# Должен вернуть: HTTP/1.1 301 Moved Permanently

curl -I https://e-novolunie.ru
# Должен вернуть: HTTP/2 200
```

### 3. Проверка с помощью telnet/nc

```bash
# С вашего компьютера
telnet 85.239.44.197 80
# Или
nc -zv 85.239.44.197 80

telnet 85.239.44.197 443
# Или
nc -zv 85.239.44.197 443
```

## Если порты всё ещё недоступны

### 1. Файрвол провайдера

Многие хостинг-провайдеры имеют собственный файрвол в панели управления. Нужно открыть порты там:

- **Timeweb**: Панель управления → Сервер → Файрвол
- **Selectel**: Панель управления → Сеть → Файрвол
- **DigitalOcean**: Networking → Firewalls
- **AWS**: Security Groups
- **Google Cloud**: VPC Network → Firewall rules
- **Azure**: Network Security Groups

### 2. Группы безопасности в облаке

Если сервер в облаке (AWS, Google Cloud, Azure), проверьте Security Groups / Firewall rules:

- Убедитесь, что правила разрешают входящий трафик на порты 80 и 443
- Проверьте, что правила применены к вашему серверу

### 3. Docker контейнер не запущен

```bash
cd ~/novolunie
docker compose ps

# Если контейнер не запущен
docker compose up -d

# Проверьте логи
docker compose logs web
```

### 4. Системный Nginx занимает порт

```bash
# Проверьте, не запущен ли системный Nginx
sudo systemctl status nginx

# Если запущен, остановите его
sudo systemctl stop nginx
sudo systemctl disable nginx

# Перезапустите Docker контейнер
cd ~/novolunie
docker compose restart
```

## Полная диагностика

Если проблема не решена, выполните полную диагностику:

```bash
cd ~/novolunie
chmod +x fix-port-80.sh
./fix-port-80.sh
```

Этот скрипт проверит все возможные причины блокировки портов.

## Частые команды

```bash
# Проверить статус UFW
sudo ufw status verbose

# Посмотреть все правила UFW
sudo ufw status numbered

# Удалить правило UFW (если нужно)
sudo ufw delete <номер_правила>

# Проверить правила iptables
sudo iptables -L INPUT -n -v

# Проверить, что слушается на портах
sudo ss -tlnp | grep -E ":(80|443) "
sudo netstat -tlnp | grep -E ":(80|443) "

# Проверить процессы на портах
sudo lsof -i :80
sudo lsof -i :443
```
