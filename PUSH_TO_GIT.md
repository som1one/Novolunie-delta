# 📤 Как запушить изменения в Git

## ⚠️ Важно: Правильный workflow

**Обычно:**
1. Изменения делаются **локально** (на вашем компьютере)
2. Коммит и пуш **локально**
3. На сервере делается **pull** (подтягивание изменений)

**НЕ рекомендуется** коммитить с сервера, так как:
- Сервер - это production (рабочая среда)
- Может быть конфликт с локальными изменениями
- Сложнее отслеживать изменения

---

## 🚀 Способ 1: С локальной машины (РЕКОМЕНДУЕТСЯ)

### Если вы на Windows (локально):

```bash
cd D:\PycharmProjects\deltasite
git add .
git commit -m "Описание изменений"
git push origin main
```

### Затем на сервере подтяните изменения:

```bash
cd ~/novolunie
git pull origin main
```

---

## 🖥️ Способ 2: С сервера (если нужно)

### Автоматический скрипт:

```bash
cd ~/novolunie
git pull origin main
chmod +x push-to-git.sh
./push-to-git.sh
```

Скрипт:
1. Покажет изменения
2. Спросит подтверждение
3. Попросит ввести сообщение коммита
4. Закоммитит и запушит

---

### Вручную с сервера:

```bash
cd ~/novolunie

# 1. Проверить изменения
git status

# 2. Добавить все изменения
git add .

# 3. Закоммитить
git commit -m "Описание изменений"

# 4. Запушить
git push origin main
```

---

## 🔐 Настройка Git на сервере (если еще не настроено)

Если при пуше просит логин/пароль:

### Вариант 1: Personal Access Token (GitHub)

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Скопируйте токен
4. При пуше используйте токен вместо пароля

### Вариант 2: SSH ключ (рекомендуется)

```bash
# На сервере
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub
```

Скопируйте публичный ключ и добавьте в GitHub:
- Settings → SSH and GPG keys → New SSH key

Затем измените remote URL:
```bash
git remote set-url origin git@github.com:som1one/Novolunie-delta.git
```

---

## 📋 Быстрые команды

### Локально (Windows):
```bash
git add .
git commit -m "Update"
git push
```

### На сервере (подтянуть):
```bash
cd ~/novolunie
git pull origin main
```

---

## ⚠️ Если есть конфликты

### На сервере (отменить локальные изменения):
```bash
cd ~/novolunie
git reset --hard HEAD
git pull origin main
```

### Или использовать скрипт:
```bash
cd ~/novolunie
git pull origin main
chmod +x update-from-git.sh
./update-from-git.sh
```

---

## ✅ Рекомендация

**Всегда работайте так:**
1. Изменения → Локально
2. Коммит → Локально  
3. Пуш → Локально
4. Pull → На сервере

Это безопаснее и правильнее! 🎯
